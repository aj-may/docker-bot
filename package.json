{
  "name": "docker-bot",
  "version": "1.0.0",
  "description": "An example repo using node, botkit, and docker.",
  "repository": "https://github.com/aj-may/docker-bot",
  "main": "dist/index.js",
  "dependencies": {
    "botkit": "^0.4.5",
    "nodemon": "^1.10.2"
  },
  "scripts": {
    "start": "node dist/index.js",
    "start-dev": "nodemon --delay 1 dist/index.js",
    "build": "babel src -d dist",
    "watch": "babel src -d dist --watch",
    "lint": "eslint src"
  },
  "author": "A.J. May",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "eslint": "^3.10.1",
    "eslint-config-airbnb": "^13.0.0",
    "eslint-plugin-import": "^2.2.0"
  }
}
